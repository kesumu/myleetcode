#pragma once
#include <string>
#include <vector>

using namespace std;

class Solution_letter_combinations_of_a_phone_number {
public:
	vector<string> letterCombinations(string digits);
	void test(void);
};